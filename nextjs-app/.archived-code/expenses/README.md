# Archived: /dashboard/expenses

**Дата архивации:** 2025-11-06
**Причина:** Функционал дублировал `/dashboard/car-expenses` (Расходы на авто)

## Содержимое

Этот каталог содержит код старого маршрута `/dashboard/expenses`, который был заменён на `/dashboard/car-expenses`.

### Файлы:
- `page.tsx` - Главная страница списка расходов
- `new/page.tsx` - Страница создания нового расхода
- `new/ExpenseForm.tsx` - Форма добавления расхода

## Что было удалено из проекта:

1. **Sidebar** (`/components/Sidebar.tsx`):
   - Удалена строка: `{ icon: '💵', label: 'Расходы', href: '/dashboard/expenses' }`

2. **Dashboard** (`/app/dashboard/page.tsx`):
   - Удалена карточка "Добавить расход" с иконкой 💵

## Альтернатива

Используйте вместо этого:
- **Маршрут:** `/dashboard/car-expenses`
- **Название:** "Расходы на авто" / "Car Expenses"
- **Функционал:** Полноценный учёт расходов с категориями (заправка, ремонт, ТО, страховка, и т.д.)

## Восстановление

Если нужно восстановить этот функционал:

1. Скопируйте файлы из `.archived-code/expenses/` обратно в `app/dashboard/expenses/`
2. Добавьте ссылку в Sidebar (строка 27)
3. Добавьте карточку на Dashboard (после строки 266)
