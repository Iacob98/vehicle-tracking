Отлично. Вот **чистая и минимальная структура расходов**, строго разделённая:

---

## 🚘 **Расходы, относящиеся к автомобилю**

### Таблица: `car_expenses`

| Поле          | Тип данных    | Описание                                  |
| ------------- | ------------- | ----------------------------------------- |
| `id`          | UUID / SERIAL | Уникальный ID                             |
| `car_id`      | UUID          | Связь с автомобилем                       |
| `date`        | DATE          | Дата расхода                              |
| `category`    | ENUM          | Категория (см. ниже)                      |
| `amount`      | NUMERIC(10,2) | Сумма (€)                                 |
| `description` | TEXT          | Комментарий (например: «замена тормозов») |
| `file_url`    | TEXT (URL)    | Чек/счёт/акт (по желанию)                 |
| `created_at`  | TIMESTAMP     | Время добавления                          |

#### Категории (`category`):

* `repair` — ремонт
* `maintenance` — плановое техобслуживание (ТО)
* `fuel` — топливо
* `insurance` — страховка
* `toll` — платные дороги
* `car_wash` — мойка
* `other` — прочее

---

## 🧑‍🔧 **Расходы, относящиеся к бригаде**

### Таблица: `brigade_expenses`

| Поле          | Тип данных    | Описание                           |
| ------------- | ------------- | ---------------------------------- |
| `id`          | UUID / SERIAL | Уникальный ID                      |
| `brigade_id`  | UUID          | Связь с бригадой                   |
| `date`        | DATE          | Дата                               |
| `category`    | ENUM          | Категория (см. ниже)               |
| `amount`      | NUMERIC(10,2) | Сумма (€)                          |
| `description` | TEXT          | Комментарий                        |
| `file_url`    | TEXT (URL)    | Фото сломанного инструмента, штраф |
| `created_at`  | TIMESTAMP     | Время добавления                   |

#### Категории (`category`):

* `broken_equipment` — поломка оборудования
* `fine` — штраф, наложенный на бригаду

---

## ✅ Особенности:

* Все записи связаны напрямую с `car_id` или `brigade_id`
* Вся аналитика по затратам строится на этих двух таблицах
* Добавление через форму в интерфейсе, с загрузкой файла (по желанию)

---

Если всё корректно — могу сразу перейти к генерации **SQL-таблиц** или **описания API**. Готов продолжать.
